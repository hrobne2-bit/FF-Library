import { Character } from '../types';

// This file is now deprecated in favor of dynamic loading from /public/chars/
// It is kept as an empty export to avoid breaking existing imports during transition
export const characters: Character[] = [];
